# Admin panel — DEVELOPMENT (used by `npm run dev` / vite dev server).
# Backend runs on a separate origin in dev, so point at it explicitly.
VITE_API_BASE_URL=https://astronaut-bagginess-landmark.ngrok-free.dev

# Google OAuth Client ID (must match GOOGLE_CLIENT_ID in the backend .env.development).
VITE_GOOGLE_CLIENT_ID=812183271103-q0d9990umrlh4m0tko6mbe1jbqkt4k3a.apps.googleusercontent.com
